# === 全局应用配置 ===
# 项目名称，将显示在API文档标题等处
PROJECT_NAME="My Fullstack App"
# API路由的统一前缀，例如 /api/v1
API_PREFIX="/api"
# 调试模式开关 (True/False)
# 在开发环境中通常设置为True，以便获取更详细的错误信息和启用热重载等功能。
# 在生产环境中应设置为False。
DEBUG=True

# === 后端服务配置 ===
# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# 控制应用记录日志的详细程度。开发时可用DEBUG，生产时建议INFO或WARNING。
LOG_LEVEL="INFO"

# CORS (Cross-Origin Resource Sharing) 配置
# 指定允许访问后端API的来源域名列表，用逗号分隔。
# 例如: ALLOWED_ORIGINS="http://localhost:3000,https://yourdomain.com"
# 如果为空或未设置，后端 FastAPI 应用可能默认允许所有来源（取决于具体实现）。
# 为了安全，生产环境建议明确指定允许的域名。
ALLOWED_ORIGINS="http://localhost:3000"

# === Neo4j 数据库配置 ===
# Neo4j数据库的连接URI。
# 在Docker Compose环境通常使用服务名（例如 'neo4j'）作为主机名。
NEO4J_URI="bolt://neo4j:7687"

# Neo4j数据库的用户名。
# 此设置会覆盖 docker-compose.yml 中 neo4j 服务里 NEO4J_AUTH 的用户部分默认值。
NEO4J_USER="neo4j"

# Neo4j数据库的密码。
# 此设置会覆盖 docker-compose.yml 中 neo4j 服务里 NEO4J_AUTH 的密码部分默认值。
# 强烈建议修改为强密码！
NEO4J_PASSWORD="s3cr3t" # 请修改为您的安全密码

# Neo4j数据库名称。
# 对于Neo4j 4.x版本，默认数据库通常是 'neo4j'。
# 对于Neo4j 5.x及更高版本，如果您想使用非默认数据库，可能需要在此处指定。
NEO4J_DATABASE="neo4j"

# === 其他服务或API密钥占位符 ===
# 例如:
# SOME_EXTERNAL_API_KEY="YOUR_API_KEY_HERE"
# AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY"
# AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_KEY"

# === 前端特定环境变量 (如果通过Docker Compose传递给前端) ===
# 通常前端环境变量以 VITE_ (对于Vite项目) 或 REACT_APP_ (对于Create React App项目) 开头。
# 这些变量会直接内联到前端构建包中，所以不要存放敏感信息。
# VITE_APP_TITLE="My Awesome Frontend"
# VITE_FEATURE_FLAG_XYZ="true"
# VITE_API_BASE_URL="/api" # 这个通常在vite.config.js中通过proxy配置，但如果前端代码直接使用也可以放在这里。
                           # 由于我们已在vite.config.ts中配置了代理，前端应直接请求相对路径如 /api/users
                           # 因此这个变量可能不是必需的，除非有其他用途。
