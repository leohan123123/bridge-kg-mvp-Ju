# 前端环境变量
VITE_API_BASE_URL=http://localhost:8000
VITE_NODE_ENV=development

# 后端环境变量
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=s3cr3t
# NEO4J_DATABASE="neo4j" # This was in the old file, uncomment if needed

# 应用配置
APP_NAME="Bridge KG MVP" # Kept quotes as it's a common practice for strings with spaces
APP_VERSION=1.0.0
DEBUG=true

# 其他可能需要的后端变量 (从旧文件保留，如果需要请取消注释)
# LOG_LEVEL="INFO"
# ALLOWED_ORIGINS="http://localhost:3000"
# API_PREFIX="/api" # VITE_API_BASE_URL is for frontend, API_PREFIX might be for backend routing

# === 其他服务或API密钥占位符 ===
# DEEPSEEK_API_KEY=YOUR_DEEPSEEK_API_KEY
# OLLAMA_API_URL=http://localhost:11434/api/chat
# OLLAMA_DEFAULT_MODEL=qwen2:0.5b
