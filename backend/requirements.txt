fastapi>=0.70.0,<0.110.0 # 固定一个合适的范围，避免不兼容的更新
uvicorn[standard]>=0.15.0,<0.28.0 # standard包含了cython加速的依赖
pydantic>=1.9.0,<2.0.0 # Pydantic V2 有较大变动，先固定在V1
python-dotenv>=0.19.0,<1.1.0 # 用于加载 .env 文件
neo4j>=4.4.0,<6.0.0 # Neo4j Python 驱动，放宽到<6以获取更多更新
ezdxf>=1.0.0,<1.5.0 # 添加 ezdxf
PyMuPDF>=1.23.0,<1.24.0 # 添加 PyMuPDF (fitz)
rich>=13.0.0,<15.0.0 # 用于美化命令行输出和日志

# gunicorn # 如果在生产环境使用 Gunicorn，取消注释

# 开发和测试依赖
pytest>=7.0.0,<8.0.0
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0
httpx>=0.25.0,<0.27.0 # 用于测试 FastAPI 应用，建议添加
python-multipart
pytest-asyncio
