fastapi>=0.70.0,<0.100.0 # 固定一个合适的范围，避免不兼容的更新
uvicorn[standard]>=0.15.0,<0.25.0 # standard包含了cython加速的依赖
pydantic>=1.9.0,<2.0.0 # Pydantic V2 有较大变动，先固定在V1
python-dotenv>=0.19.0,<1.0.0 # 用于加载 .env 文件
neo4j>=4.4.0,<5.0.0 # Neo4j Python 驱动
# gunicorn # 如果在生产环境使用 Gunicorn，取消注释

# 开发和测试依赖 (可选，如果Dockerfile中不安装dev依赖，可以分开管理)
# pytest
# httpx # 用于测试 FastAPI 应用
